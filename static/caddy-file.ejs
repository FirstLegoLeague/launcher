{
	admin off
}

:<%= configPort %> {
    root "<%= envDir %>"
    file_server
}

<% sites.forEach(site => { -%>
:<%= site.port %> {
    root "<%= site.root %>"
    redir /environment.json :<%= configPort %>/<%= site.id %>.json permanent
<% if(site.index) { -%>
    index <%= site.index %>
<% } -%>
    file_server
}
<% }) -%>